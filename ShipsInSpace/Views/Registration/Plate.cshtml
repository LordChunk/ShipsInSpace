@{
    ViewData["Title"] = "Register";
}
@using ShipsInSpace.Models.Enums
@model ShipsInSpace.Models.RegistrationViewModel

<div class="text-center">
    <form asp-action="Plate">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <div class="form-group">
            <label  asp-for="Plate" class="control-label"></label>
            <input asp-for="Plate" name="Plate" class="form-control" />
            <span asp-validation-for="Plate" class="text-danger"></span>
        </div>
        
        <div class="form-group">
            <label asp-for="PilotLicense" class="control-label"></label>
            <select asp-for="PilotLicense" asp-items="Html.GetEnumSelectList<PilotLicense>()"></select>
            <span asp-validation-for="PilotLicense" class="text-danger"></span>
        </div>

        <div class="form-group">
            <input type="submit" value="Save" class="btn btn-primary" />
        </div>
    </form>
</div>
